using DashDetective.Shared;

namespace DashDetective.Tabs.Processes;

/// <summary>
/// The Processes tab: a Task-Manager-style live process view. This is the Phase 0 placeholder — it
/// establishes the tab, its navigation entry and the shell routing so the page renders. The live,
/// grouped process table (Apps / Background), the summary strip, sortable headers, End task and native
/// Properties arrive in later phases (see the plan at
/// <c>C:\Users\User\.claude\plans\processes-tab-plan.md</c>).
/// </summary>
public partial class ProcessesViewModel : ViewModelBase {
}
