namespace DashDetective.Services.Theming;

/// <summary>The application colour scheme the user can choose in Settings → Appearance.</summary>
public enum AppTheme {
    /// <summary>Follow the operating-system light/dark setting.</summary>
    System,
    Light,
    Dark,
}
