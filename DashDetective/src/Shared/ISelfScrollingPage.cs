namespace DashDetective.Shared;

/// <summary>A page that fills the viewport and manages its own internal scrolling,
/// so the shell must NOT wrap it in a scrolling region.</summary>
public interface ISelfScrollingPage { }
